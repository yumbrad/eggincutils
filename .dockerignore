# flyctl launch added from .gitignore
# Keep .git in the Docker context: Dockerfile runs scripts/assert-clean-git.sh
# so raw `fly deploy` cannot publish uncommitted local changes.
**/.next
**/node_modules
**/npm-debug.log*
**/.DS_Store
**/*.tsbuildinfo
**/local_data

# Sibling checkout of the separate yumworkout Fly app, kept locally for the
# /workout proxy work; not part of this app's build.
/yumworkout

# Emacs temp/lock/autosave files
**/*~
**/#*#
**/.#*
